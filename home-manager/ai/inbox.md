List of good tools to use.

Verbose mode on the logs of running services (e.g. pocketbase) and a way for the AI to have access to these logs at runtime so that it can self-debug rather than me copy-pasting things over all the time. But how do I do this in a token and context efficient way or? When I copy paste I'm very selective. So I must give the AI the ability to search and filter the logs, rathe than just get everything. Or better, use an ephemeral sub-agent with a different context window which can query the fuller logs to give the main agent more concise info. This does not solve for token efficiency that much, but it does keep the main context window very clean. 

This directory contains a bunch of projects. I want to back it up by moving it to a flash drive so I can wipe this computor. But I don't want to copy any build
  aretifacts like rust target dirs, node_modules, python venvs, etc etc etc. Can you help me find and one by one delete these?

There is definitly also something in using the git commit messages for project memory / context collection.

